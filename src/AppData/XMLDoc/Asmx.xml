<TngnWsvc>

    <Class name="Asmx">

        <!--
            <include file='AppData/XmlDoc/Asmx.xml' path='TngnWsvc/Class[@name="Asmx"]/ClassDescription/*'/>
        -->
        <ClassDescription>
            <para>
                This class doesn't do much actual work, and should remain fairly static. For the most part, it just hands information to
                <see href="https://github.com/spectrum-health-systems/outpost31"> Outpost31</see>, where the heavy lifting is done.
                <br/>
                There are two methods in this class which <i>are required</i> and <i>should not be modified</i>:
                <list type="bullet">
                    <item><see cref="GetVersion()"/></item>
                    <item><see cref="RunScript(OptionObject2015, string)"/></item>
                </list>
            </para>
        </ClassDescription>

        <!--
            <include file='AppData/XmlDoc/Asmx.xml' path='TngnWsvc/Class[@name="Asmx"]/RunScript/*'/>
        -->
        <RunScript>
            <para>
                <list type="bullet">
                    <item>
                        If Avatar doesn't pass an <paramref name="origOptObj"/> or <paramref name="origScriptParam"/>, the Tingen Web
                        Service will fail, and a critical log will be created.
                    </item>
                    <item>
                        If the TngnWsvcMode must be set to <c>enabled</c>, <c>disabled</c>, or <c>passthrough</c>. Any other mode will
                        behave as <c>disabled</c>.
                    </item>
                    <item>
                        There is a special "AdminMode" that can be set in the Web.Config file, but this is only for administrative/development
                        tasks. Additional information about this mode can be found in the Tingen Web Service Manual.
                    </item>
                </list>
            </para>
        </RunScript>

    </Class>

</TngnWsvc>